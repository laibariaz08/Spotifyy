<!-- res/values/styles.xml -->
<resources>
    <style name="NoActiveIndicatorBottomNav" parent="Widget.MaterialComponents.BottomNavigationView">
<!--        <item name="materialThemeOverlay">@style/NoActiveIndicatorTheme</item>-->
    </style>
    <style name="MyCustomMenuTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textSize">18sp</item>  <!-- Adjust the size as needed -->
        <item name="android:textColor">@android:color/white</item>  <!-- Set text color -->
    </style>
</resources>
